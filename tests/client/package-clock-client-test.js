
import './helper-electron.js';

import app, { KioskClock } from '../../packages/clock/clock-client.js';

import { fn } from './helper-main.js';

describe(fn(import.meta.url), () => {
	it('should react to events', function () {

	});
});
