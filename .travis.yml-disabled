os: linux
dist: focal

language: node_js
node_js:
  - "14"

addons:
  apt:
    packages:
    - exiv2
    - libexiv2-dev
    - cifs-utils
    - libgconf-2-4
    - chromium-browser

# cache:
#   npm: true
#   directories:
#   - ~/.cache

install:
  - make build

script:
  - make test
  - make lint
